# AI 使用说明

## 1. 使用的 AI 工具

- Claude Code（Anthropic Claude，DeepSeek V4 Pro 模型）
- 运行环境：Windows 11，Jupyter Notebook（Python 3.10+）

## 2. 使用 AI 的具体环节

### 阶段一：Notebook 框架搭建

| 子任务 | AI 贡献 | 本人确认/修改 |
|--------|---------|--------------|
| Notebook 结构规划 | 根据考试说明设计 8 章框架（摘要→数据读取→基本情况→质量检查→清洗→统计→可视化→结论） | 确认符合 10 项评分标准 |
| 中文字体跨平台方案 | 自动检测 win32/darwin/linux 系统字体，代码无需修改即可在 macOS/Linux 运行 | 确认在 Windows 下正常渲染 |

### 阶段二：数据探查与清洗

| 子任务 | AI 贡献 | 本人确认/修改 |
|--------|---------|--------------|
| 数据格式探查 | 编写探查脚本，逐字段查看格式、缺失值、唯一值 | 理解每个字段的实际含义 |
| 正则提取工具函数 | 生成 `extract_num()`、`safe_extract()` 通用清洗函数 | 测试边界情况（空值、异常格式） |
| 6 大字段清洗 | 面积、总价、均价、户型、楼层、时间、方位 → 9 个数值变量 + 3 个分类变量 | 将 `面积_㎡` 与预清洗 `面积数值` 逐行比对，差异为 0 |
| 缺失值处理 | 识别 26 条价格缺失记录（0.22%），显式 `dropna` 排除 | 确认缺失记录属于同一批次房源 |
| **异常值检测** | 按市区分组计算 IQR，检测均价和总价的异常值数量和占比 | 确认异常值集中在鼓楼区高端学区房，属于真实市场信号而非数据错误 |

### 阶段三：统计分析

| 子任务 | AI 贡献 | 本人确认/修改 |
|--------|---------|--------------|
| 11 个统计模块 | 市场概况、市区对比、户型分布、面积段、房龄段、朝向、楼层、交叉透视、相关性等 | 逐模块确认统计逻辑正确 |
| Pearson 相关系数 | 面积-总价-均价-房龄的 Pearson 矩阵 | 核对 r=0.64 等关键数值与 `df.describe()` 一致 |
| **Spearman 秩相关** | 对右偏分布增加 Spearman 秩相关系数，对比 Pearson 差异 | 确认 Spearman 更适合房价数据的右偏特性 |

### 阶段四：可视化

| 子任务 | AI 贡献 | 本人确认/修改 |
|--------|---------|--------------|
| 图表 1：市区供需双轴图 | 水平柱状图 + twiny 散点叠加 | 调整配色方案和标签位置 |
| 图表 2：价格分布直方图 | 总价+均价双直方图，标注中位数/均值/偏度/峰度 | 调整 `figsize`、`alpha` 透明度；**增加 KDE 密度曲线覆盖层** |
| 图表 3：市区箱线图 | Seaborn boxplot，按中位数排序，标注异常点 | 调整 `flierprops` 让离群点更清晰 |
| 图表 4：面积-总价散点图 | 散点图 + 线性回归线，按 top5 市区分色 | 调整散点透明度和大小；**修复 SettingWithCopyWarning** |
| 图表 5：户型分布柱状图 | Top 12 户型 viridis 配色柱状图 | 确认标签和百分比标注 |
| 图表 6：房龄-均价箱线图 | 6 个房龄段的均价分布 | 增加样本量标注 |
| **相关系数热力图** | Pearson + Spearman 双面板热力图（`RdBu_r` 配色，annot=True） | 确认颜色映射和数值标注 |

### 阶段五：执行验证与迭代优化

| 子任务 | AI 贡献 | 本人确认/修改 |
|--------|---------|--------------|
| 全量执行验证 | `jupyter nbconvert --execute` 自动化执行并捕获错误 | 确认零报错、图表完整 |
| 变量名 Bug 修复 | 修正 KDE 代码中 `unit_price_plot` → `unit_plot` 的变量名错误 | 重新执行通过 |
| 跨平台兼容性确认 | 验证相对路径、编码探测、字体检测、PNG 嵌入均可跨环境打开 | 确认文件 1.0 MB，39 个代码 cell 全部有输出 |

## 3. 主要提问内容摘要

1. "先看看 CSV 的实际字段格式是什么样的"
   → 探查脚本，发现面积格式 `103㎡`、户型格式 `3 室 2 厅 2 卫` 等

2. "跑完整的统计分析，拿到所有描述性统计和分组统计结果"
   → 生成 11 个统计模块

3. "根据真实数据把完整 Notebook 写出来，包含 6 张图"
   → 生成 72 个 Cell 的完整 Notebook

4. "执行验证，确保 Kernel → Restart & Run All 无报错"
   → `jupyter nbconvert --execute` 通过

5. "检查是否需要添加异常值检查步骤，并告诉我有没有地方可以再优化"
   → 新增 IQR 异常值检测、Spearman 相关、热力图、KDE 曲线、修复警告

6. "这份文件是否能在非本地环境中打开并保证内容完全一致"
   → 全量重新执行，base64 PNG 嵌入，确认跨平台兼容

## 4. 我对 AI 输出做了哪些检查、修改或取舍

1. **清洗代码验证**：将清洗后的 `面积_㎡` 与数据集中预清洗的 `面积数值` 列逐行比对，差异为 0，确认正则提取完全正确；
2. **统计结果核实**：将 AI 生成的统计摘要中的关键数字（总价中位数 173 万、均价中位数 18,326 元/㎡、Pearson r=0.64 等）与 `df.describe()` 原始输出逐一核对；
3. **图表排版调整**：调整了图片尺寸（`figsize`）、散点透明度（`alpha`）、颜色方案（`palette`），确保图表在 Notebook 中清晰可读；
4. **地段>房龄 结论验证**：这个反直觉发现（20年以上老旧小区均价高于 10-15年）通过透视表和箱线图双重验证后才写入结论；
5. **KDE 变量名修正**：AI 添加 KDE 密度曲线时误将 `unit_plot` 写成 `unit_price_plot`，导致执行报错，手动修正后重新执行；
6. **异常值策略决策**：AI 建议删除或缩尾异常值，我判断房产数据的极端值（学区房 78,453 元/㎡）是真实市场信号，决定保留并在报告中优先使用中位数。

## 5. 哪些分析结论是我根据数据自行判断得到的

1. **三级市场格局**的划分（鼓楼→近郊→远郊）——我在查看市区统计表后，将 12 个市区归纳为核心/近郊/外围三个梯队；
2. **供给与价格空间错位**的观察——晋安房源最多但均价仅第三，是我对比房源数量与均价排名后发现的规律；
3. **大户型定价逻辑发散**的判断——散点图 150㎡ 以上发散明显，我结合福州实际情况（豪宅、别墅产品异质性）解释了这一现象；
4. **异常值保留策略**——AI 建议清除或缩尾异常值，我基于房产市场知识判断学区房等极端值是真实信号，决定保留；
5. 报告中对数据局限性的讨论（挂牌价≠成交价、幸存者偏差、关键变量缺失等）是基于统计学课程所学方法论的个人判断。

---

*本说明真实记录了 AI 辅助过程。所有代码逻辑、统计方法和最终结论均经本人理解、核实和确认。*
